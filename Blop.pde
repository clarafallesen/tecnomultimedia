class Blop{

  // -----------------------------------------------------
  // CAMPOS (variables) #propiedes
  // Posición
  
  
  // -----------------------------------------------------
  // CONSTRUCTOR > metodo/funcion inicial (el "setup" del objeto)
    // inicializar posicion


  // -----------------------------------------------------
  // METODOS (funciones) #acciones
  // dibujar()
  // --mostrar los sprites de mi personaje (todavia no se si voy a usar sprites)

  // METODOS para KeyPressed()
  // moverIzq()
  // --mueve al personaje hacia la izquierda con la flecha correspondiente
  
  // moverDer()
  // --mueve al personaje hacia la derecha con la flecha correspondiente

  // moverAr()
  // --mueve al personaje hacia la arriba con la flecha correspondiente
  
  // moverAb()
  // --mueve al personaje hacia abajo con la flecha correspondiente


}
